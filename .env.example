# WorldCup Data Platform — variáveis de ambiente
# Copiar para .env (nunca commitar .env)

# football-data.org (free tier — fixtures Copa 2026)
FOOTBALL_DATA_API_KEY=sua_key_aqui

# Databricks (CLI upload)
DATABRICKS_HOST=https://community.cloud.databricks.com
DATABRICKS_TOKEN=seu_token_aqui
